<nav class="navbar navbar-expand-lg navbar-dark" style="background-color: black;">
  <div class="container-fluid">
    <%= link_to "www.foodspictures.cl", root_path, class: "navbar-brand colorbrand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Inicio", root_path, class: "nav-link active" %>
        </li>
        <li class="nav-item">
          <%= link_to "Nueva Food's Picture", new_post_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Tus Tablas", boards_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Ver Etiquetas", tags_path, class: "nav-link" %>
        </li>
      </ul>
      <div>
        <% if user_signed_in? %>
          <%= button_to( "Salir", destroy_user_session_path, method: :delete, class: "btn btn-danger btn-sm" ) %>
        <% else %>
          <%= link_to "Registrar", new_user_registration_path, class: "btn btn-warning btn-sm" %>
          <%= link_to "Ingresar", new_user_session_path, class: "btn btn-success btn-sm"  %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
  <form class="d-flex">
    <%= form_with url: posts_path, method: :get do |f| %>
      <%= f.text_field :search, :class => "form-control me-2", placeholder: "Buscar Food's Picture" %>
      <%= f.submit "Buscar", :class => "btn btn-info" %>
    <% end %> 
  </form>
<%= image_tag "banner2.png", class: "w-100 mt-3" %>
<% if user_signed_in? %>
  <span class="lead colorbrand" >¡Hola <%= current_user.name %>!</span>
<% end %>